#include "chess.h"

int main()
{
    Board grid;
    
    grid.build();
    grid.set();
    // grid.printCoords();

    grid.print();

    // Set the board (with pawns)

    // Program the pieces
        // Move by coordinates. 

        // Chess piece can only move in allowable directions
}